;;; app/app.el --- doom :app group file  -*- lexical-binding: t; -*-
;;; Module files in this dir are gated by `(modulep! :app <module>)';
;;; this group file always loads first.

;;; app-config.el --- calendar, irc, rss, doom :app set  -*- lexical-binding: t; -*-

;;; app/app.el ends here
