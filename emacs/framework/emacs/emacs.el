;;; emacs/emacs.el --- doom :emacs group file  -*- lexical-binding: t; -*-
;;; Module files in this dir are gated by `(modulep! :emacs <module>)';
;;; this group file always loads first.

;;; emacs-config.el --- doom :emacs (builtin conveniences)  -*- lexical-binding: t; -*-

;;; emacs/emacs.el ends here
