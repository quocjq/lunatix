;;; term/term.el --- doom :term group file  -*- lexical-binding: t; -*-
;;; Module files in this dir are gated by `(modulep! :term <module>)';
;;; this group file always loads first.

;;; term-config.el --- doom :term (eshell + vterm)  -*- lexical-binding: t; -*-

;;; term/term.el ends here
