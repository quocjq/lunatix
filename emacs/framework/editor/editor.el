;;; editor/editor.el --- doom :editor group file  -*- lexical-binding: t; -*-
;;; Module files in this dir are gated by `(modulep! :editor <module>)';
;;; this group file always loads first.

;;; editor-config.el --- doom :editor (format, whitespace, snippets, fold)  -*- lexical-binding: t; -*-

;;; editor/editor.el ends here
