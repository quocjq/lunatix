;;; config/config.el --- doom :config group file  -*- lexical-binding: t; -*-
;;; Module files in this dir are gated by `(modulep! :config <module>)';
;;; this group file always loads first.

;;; config-config.el --- doom :config defaults  -*- lexical-binding: t; -*-

;; Ported from doom modules/config/default. The <leader> tree lives in
;; keybindings-config.el; this file carries the non-keybinding defaults,
;; the smartparens/avy/link-hint config, and doom's smart-backspace + comment
;; continuation.

;;; config/config.el ends here
